# Minimal runtime deps for the daily refresh job (no API/web-server extras).
# The refresh uses SQLite, so no Postgres driver is needed here.
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0
openpyxl>=3.1
SQLAlchemy>=2.0
python-dotenv>=1.0
tenacity>=8.2
